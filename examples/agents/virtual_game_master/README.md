# Archived Virtual Game Master Demo

This folder contains an older application-style demo. It is kept as historical
code only and is not part of the maintained examples path.

The `v1` implementation still references legacy state APIs such as
`ContextAppState`. New agent examples should use `ChatToolAgent` or
`AgentHarness` directly, and should prefer `../navigable_memory/` for memory
workflows.

