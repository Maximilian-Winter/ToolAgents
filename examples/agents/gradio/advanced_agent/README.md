# Legacy Gradio AdvancedAgent Demo

This Gradio demo uses the legacy `AdvancedAgent` stack and is not part of the
maintained example path.

For new UI demos, prefer composing `ChatToolAgent` or `AgentHarness` directly
with explicit tools and `NavigableMemory` where memory is needed.
